@page "/estudiantes/crear"
@using Microsoft.AspNetCore.Authorization
@using LibroManager.Services.Interfaces
@using LibroManager.DTOs
@using LibroManager.Constants
@inject IEstudianteService EstudianteService
@inject NavigationManager NavigationManager
@attribute [Authorize(Policy = RoleConstants.Permissions.Estudiantes.Create)]
@rendermode InteractiveServer

<PageTitle>Crear Estudiante - LibroManager</PageTitle>

<div class="container">
    <h1>Crear Nuevo Estudiante</h1>

    <div class="row">
        <div class="col-md-6">
            <EditForm Model="@estudianteDto" OnValidSubmit="@HandleValidSubmit">
                <DataAnnotationsValidator />

                <div class="mb-3">
                    <label for="nombre" class="form-label">Nombre</label>
                    <InputText id="nombre" class="form-control" @bind-Value="@estudianteDto.Nombre" />
                    <ValidationMessage For="@(() => estudianteDto.Nombre)" />
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <InputText id="email" class="form-control" @bind-Value="@estudianteDto.Email" />
                    <ValidationMessage For="@(() => estudianteDto.Email)" />
                </div>

                <div class="mb-3">
                    <button type="submit" class="btn btn-primary me-2">
                        <i class="bi bi-save"></i> Guardar
                    </button>
                    <button type="button" class="btn btn-secondary" @onclick="@Cancelar">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </button>
                </div>
            </EditForm>

            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <div class="alert alert-danger mt-3">
                    @errorMessage
                </div>
            }
        </div>
    </div>
</div>

@code {
    private EstudianteCreateDTO estudianteDto = new();
    private string errorMessage = string.Empty;

    private async Task HandleValidSubmit()
    {
        try
        {
            var result = await EstudianteService.CreateAsync(estudianteDto);
            if (result)
            {
                NavigationManager.NavigateTo("/estudiantes");
            }
            else
            {
                errorMessage = "No se pudo crear el estudiante";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error: {ex.Message}";
        }
    }

    private void Cancelar()
    {
        NavigationManager.NavigateTo("/estudiantes");
    }
}